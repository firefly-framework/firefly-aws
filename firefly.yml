project: firefly-aws
provider: aws

contexts:
  firefly:
  firefly_aws:
    region: ${AWS_DEFAULT_REGION}
    bucket: firefly-aws
    is_extension: true
    s3_domain_url: ${S3_DOMAIN_URL}
#    storage:
#      services:
#        s3: ~
#      default: s3

evnironments:
  dev: ~